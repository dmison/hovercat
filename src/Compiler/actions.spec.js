/* global describe it */
const expect = require('chai').expect;
const actions = require('./actions.js');

describe('testing async compiler actions', () => {
  // TODO: write the damn async tests

});
